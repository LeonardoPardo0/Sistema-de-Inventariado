# Sistema-de-Inventariado
### Ejecutar: docker-compose up --build -d
### Para ver log: docker-compose logs -f
### Para detener: docker-compose logs -f
### Debe tener instalado Docker Desktop
### El archivo de BD.txt es para conectarse a MongoBD compass (instala)
